<property>
 <name>dfs.name.dir</name>
 <value><%= (name_directories.class == Array) ? name_directories.join(',') : name_directories %></value>
</property>
<property>
 <name>dfs.data.dir</name>
 <value><%= (data_directories.class == Array) ? data_directories.join(',') : data_directories %></value>
</property>